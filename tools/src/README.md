upload_product.js
============

```
# upload to remote
make
# local test
make dev
```

write down a `local.conf.js`

    $ cd conf && touch local.conf.js

should include:

    module.exports = {
        token: 'you remote token'
    }
